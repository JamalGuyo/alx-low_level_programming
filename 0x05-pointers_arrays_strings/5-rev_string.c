#include <string.h>
#include "main.h"

/**
 * rev_string - fn to rev string
 * @s: parameter string
 * Return: void
 */
void rev_string(char *s)
{
	int i, j;
	char *sCpy = s;

	for (i = strlen(s) - 1; i >= 0; i--)
	{
		for(j = 0; j >= i; j++)
		{

	}
}
