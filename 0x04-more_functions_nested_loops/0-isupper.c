#include "main.h"

/**
 * _isupper - fn finding upper and lower case
 * @c: parameter, a character
 * Return: 0 is lower and 1 is upper
 */
int _isupper(int c)
{
	if (c >= 'a' && c <= 'z')
	{
	return (0);
	}
	return (1);
}
